import { PayloadAction } from '@reduxjs/toolkit';

const initialState: any = {

}

const Home = (state=initialState, action:PayloadAction) => {
    switch(action.type) {
        default:
            return state
    }
}

export default Home