import { PayloadAction } from '@reduxjs/toolkit';

const initialState: any = {

}

const DeFi = (state=initialState, action:PayloadAction) => {
    switch(action.type) {
        default:
            return state
    }
}

export default DeFi